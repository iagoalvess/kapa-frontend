# Ambiente dos testes. Versionado de propósito: a suíte precisa rodar num clone limpo.
# A API nunca é chamada de verdade — o MSW intercepta este endereço.
VITE_API_URL=http://api.teste
VITE_APP_NOME=Frontend
